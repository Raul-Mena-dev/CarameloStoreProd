import{requireAdmin}from"@/lib/auth";import{formatCurrency}from"@/lib/domain";export default async function Dashboard(){const{supabase:s}=await requireAdmin();const today=new Date();today.setHours(0,0,0,0);const[{count:newOrders},{count:products},{count:soldOut},{data:latest}]=await Promise.all([s.from('orders').select('*',{count:'exact',head:true}).eq('status','new'),s.from('products').select('*',{count:'exact',head:true}).eq('is_active',true),s.from('products').select('*',{count:'exact',head:true}).eq('track_stock',true).eq('stock',0),s.from('orders').select('*').order('created_at',{ascending:false}).limit(6)]);return <><div className="grid gap-4 sm:grid-cols-3">{[['Pedidos nuevos',newOrders],['Productos activos',products],['Agotados',soldOut]].map(([label,value])=><div className="card p-6" key={String(label)}><p className="text-stone-600">{label}</p><strong className="text-4xl">{value??0}</strong></div>)}</div><section className="card mt-8 overflow-hidden"><h2 className="p-6 text-xl font-black">Últimos pedidos</h2><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-cream"><tr><th className="p-4">Número</th><th>Cliente</th><th>Total</th><th>Estado</th></tr></thead><tbody>{latest?.map(o=><tr className="border-t" key={o.id}><td className="p-4 font-bold">{o.order_number}</td><td>{o.customer_name}</td><td>{formatCurrency(Number(o.total))}</td><td>{o.status}</td></tr>)}</tbody></table></div></section></>}
