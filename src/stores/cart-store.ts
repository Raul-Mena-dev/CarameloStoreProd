"use client";import{create}from"zustand";import{persist}from"zustand/middleware";
export type CartItem={productId:string;name:string;imageUrl:string|null;unitPrice:number;quantity:number};type CartState={items:CartItem[];add:(item:CartItem)=>void;remove:(id:string)=>void;setQuantity:(id:string,q:number)=>void;clear:()=>void};
export const useCart=create<CartState>()(persist((set)=>({items:[],add:item=>set(s=>{const old=s.items.find(i=>i.productId===item.productId);return{items:old?s.items.map(i=>i.productId===item.productId?{...i,quantity:Math.min(99,i.quantity+item.quantity)}:i):[...s.items,item]}}),remove:id=>set(s=>({items:s.items.filter(i=>i.productId!==id)})),setQuantity:(id,q)=>set(s=>({items:q<=0?s.items.filter(i=>i.productId!==id):s.items.map(i=>i.productId===id?{...i,quantity:Math.min(99,q)}:i)})),clear:()=>set({items:[]})}),{name:"caramelo-cart"}));
