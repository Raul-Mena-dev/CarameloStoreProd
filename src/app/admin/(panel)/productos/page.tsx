import Link from"next/link";import{requireAdmin}from"@/lib/auth";import{formatCurrency}from"@/lib/domain";export default async function AdminProducts(){const{supabase:s}=await requireAdmin();const{data}=await s.from('products').select('id,name,sku,price,stock,is_active,is_featured').order('created_at',{ascending:false});return <section className="card overflow-hidden"><div className="flex items-center justify-between p-6"><h2 className="text-2xl font-black">Productos</h2><Link className="btn-primary" href="/admin/productos/nuevo">Nuevo producto</Link></div><div className="overflow-x-auto"><table className="w-full text-left"><thead className="bg-cream"><tr><th className="p-4">Producto</th><th>Precio</th><th>Stock</th><th>Estado</th><th></th></tr></thead><tbody>{data?.map(p=><tr className="border-t" key={p.id}><td className="p-4"><strong>{p.name}</strong><small className="block">{p.sku}</small></td><td>{formatCurrency(Number(p.price))}</td><td>{p.stock??'∞'}</td><td>{p.is_active?'Activo':'Inactivo'}</td><td><Link className="font-bold underline" href={`/admin/productos/${p.id}/editar`}>Editar</Link></td></tr>)}</tbody></table></div></section>}
