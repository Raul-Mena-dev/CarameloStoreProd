import type{Product}from"@/types/database";import{ProductCard}from"./product-card";export function ProductGrid({products}:{products:Product[]}){if(!products.length)return <div className="card col-span-full p-12 text-center"><span className="text-5xl">🍬</span><h2 className="mt-4 text-xl font-black">Aún no hay dulces por aquí</h2><p className="mt-2 text-stone-600">Prueba con otros filtros o vuelve pronto.</p></div>;return <div className="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>}
