import type{MetadataRoute}from"next";import{getCategories,getProducts}from"@/lib/data";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000';const[p,c]=await Promise.all([getProducts(),getCategories()]);return[{url:base},{url:`${base}/productos`},{url:`${base}/ofertas`},...p.map(x=>({url:`${base}/productos/${x.slug}`})),...c.map(x=>({url:`${base}/categorias/${x.slug}`}))]}
