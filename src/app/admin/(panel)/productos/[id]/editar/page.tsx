import{notFound}from"next/navigation";import{requireAdmin}from"@/lib/auth";import{ProductForm}from"@/components/admin/product-form";import type{Category,Product}from"@/types/database";export default async function Edit({params}:{params:Promise<{id:string}>}){const{id}=await params;const{supabase:s}=await requireAdmin();const[{data:p},{data:c}]=await Promise.all([s.from('products').select('*').eq('id',id).single(),s.from('categories').select('*').order('name')]);if(!p)notFound();return <><h2 className="mb-6 text-3xl font-black">Editar producto</h2><ProductForm product={p as Product} categories={(c??[])as Category[]}/></>}
