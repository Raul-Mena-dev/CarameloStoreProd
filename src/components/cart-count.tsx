"use client";import{useEffect,useState}from"react";import{useCart}from"@/stores/cart-store";export function CartCount(){const[ready,setReady]=useState(false);const count=useCart(s=>s.items.reduce((n,i)=>n+i.quantity,0));useEffect(()=>setReady(true),[]);if(!ready||!count)return null;return <span className="absolute -right-1 -top-1 grid size-5 place-items-center rounded-full bg-gold text-[11px] font-black text-ink">{count}</span>}
