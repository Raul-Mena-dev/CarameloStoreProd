import{notFound}from"next/navigation";import{getCategories,getProducts}from"@/lib/data";import{ProductGrid}from"@/components/product-grid";export default async function CategoryPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const categories=await getCategories();const category=categories.find(c=>c.slug===slug);if(!category)notFound();const products=await getProducts({category:slug});return <main className="container py-12"><p className="font-black uppercase tracking-widest text-cherry">Categoría</p><h1 className="text-5xl font-black">{category.name}</h1>{category.description&&<p className="my-4 max-w-2xl text-lg text-stone-600">{category.description}</p>}<div className="mt-8"><ProductGrid products={products}/></div></main>}
